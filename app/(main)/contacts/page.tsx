import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Contacts",
};

export default function NewsPage() {
  return <section className="bg-bgColor h-dvh w-full">Contacts!</section>;
}
