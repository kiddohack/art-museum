import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Events",
};

export default function NewsPage() {
  return <section className="bg-bgColor h-dvh w-full">Events!</section>;
}
